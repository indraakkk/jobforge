# Database (managed by devenv.nix services.postgres)
DATABASE_URL=postgresql://indra@localhost:5455/jobforge

# MinIO (S3-compatible storage, managed by docker-compose)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=jobforge
MINIO_SECRET_KEY=jobforge-secret
MINIO_BUCKET=cv-files
MINIO_USE_SSL=false

# AI (Phase 4)
# ANTHROPIC_API_KEY=sk-ant-api03-...
